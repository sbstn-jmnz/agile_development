<div class="row">
  <div class="col-sm-12 text-xs-center bootstrap-welcome">
    <h1>Welcome <%= current_user.email if user_signed_in?%></h1>
    <p><%= link_to 'edit profile', edit_user_registration_path(current_user) if user_signed_in?%></p>
    <p><%= link_to 'login', new_user_session_path() unless user_signed_in?%></p>
    <p><%= link_to 'sign_in', new_user_registration_path() unless user_signed_in?%></p>
    <p><%= link_to 'logout', destroy_user_session_path(), method: :delete if user_signed_in? %></p>
  </div>
</div>


<h1>Home#index</h1>
<p>Contratar sicario precio $50.000 <%= link_to 'Comprar',  purchases_create_path() %></p>
